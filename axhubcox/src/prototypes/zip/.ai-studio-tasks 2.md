# AI Studio 项目转换任务清单

> **重要**: 请先阅读 `/skills/ai-studio-project-converter/SKILL.md` 了解转换规范

**名称**: zip
**项目位置**: `src/prototypes/zip/`
**原始文件**: `temp/google_aistudio-zip-1775568427231` (仅供参考，不要修改)
**生成时间**: 4/7/2026, 9:27:07 PM

## 📊 项目概况

- 总文件数: 12
- 组件数: 0
- Import Map: ✗ 不存在
- 自定义样式: ✗ 不存在
- 外部字体: ✗ 不存在
- 需要安装的依赖: 10 个

## ✅ 转换任务（共 5 个）

### 任务 1: 转换主应用组件

**目标**: 将 `App.tsx` 转换为本项目组件规范

⚠️ 未找到 `App.tsx`，请手动创建 `index.tsx`

### 任务 2: 创建 style.css

**目标**: 提取 index.html 中的样式信息

**操作**:
1. 创建 `style.css`，开头添加 `@import "tailwindcss";`

### 任务 3: 移除 AI Studio 特定文件

**目标**: 删除不需要的文件

**需要删除**:
- ✓ `index.html` (已提取信息)
- ⚠️ `vite.config.ts` (检查路径别名后可删除)
- ⚠️ `metadata.json` (可选保留作为参考)

### 任务 4: 安装依赖

**执行命令**:
```bash
pnpm add @google/genai @tailwindcss/vite @vitejs/plugin-react clsx dotenv express lucide-react motion tailwind-merge vite
```

**环境变量**:
⚠️ 项目使用了环境变量，需要配置 `.env.local`
```
'process.env.GEMINI_API_KEY': JSON.stringify(env.GEMINI_API_KEY),
```

### 任务 5: 验收测试

**执行命令**:
```bash
node scripts/check-app-ready.mjs /prototypes/zip
```

**验收标准**: 状态为 READY，页面正常渲染，无控制台错误

## 📚 参考资料

- **转换规范**: `/skills/ai-studio-project-converter/SKILL.md`
- **原始项目**: `temp/google_aistudio-zip-1775568427231` (仅供参考)
- **详细数据**: `.ai-studio-analysis.json`

## 💡 注意事项

1. **Import Map**: CDN 依赖需转换为 npm 包
2. **自定义样式**: 从 index.html 提取到 style.css
3. **路径别名**: 检查 vite.config.ts 中的 alias 配置
4. **环境变量**: `process.env.*` 需改为 `import.meta.env.VITE_*`
5. **原始文件**: `temp/google_aistudio-zip-1775568427231` 目录保留作为参考，不要修改
6. **验证**: 完成后务必运行验收脚本确认
